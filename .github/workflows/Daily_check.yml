name: Container Health Check

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '*/5 * * * *'

env:
  TELEGRAM_TO: -1003170352623
  CONTAINER_NAME: "bot-ebal-nahui"

jobs:
  check-container:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Проверяем контейнер
      - name: Check container status on remote server
        id: check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: false
          register: true
          script: |
            CONTAINER="${{ env.CONTAINER_NAME }}"
            echo "🔍 Checking container: $CONTAINER"

            if ! docker inspect "$CONTAINER" >/dev/null 2>&1; then
              RESULT="not_found|❌ Container $CONTAINER does not exist"
            else
              STATUS=$(docker inspect --format='{{.State.Status}}' "$CONTAINER")
              if [ "$STATUS" = "running" ]; then
                RESULT="running|🟢 Container is running"
              else
                RESULT="stopped|🔴 Container is stopped"
              fi
            fi

            echo "$RESULT"

      # 2️⃣ Разбираем результат
      - name: Parse container status
        id: parse
        run: |
          line="${{ steps.check.outputs.result }}"
          echo "Raw line: $line"
          last_line=$(echo "$line" | grep '|' | tail -n 1)
          echo "Parsed line: $last_line"
          status=$(echo "$line" | cut -d'|' -f1)
          details=$(echo "$line" | cut -d'|' -f2-)
          echo "status=$status" >> $GITHUB_OUTPUT
          echo "details=$details" >> $GITHUB_OUTPUT

      # 3️⃣ Получаем текущее время (МСК)
      - name: Get current time (Moscow)
        id: time
        run: |
          export TZ="Europe/Moscow"
          now=$(date +"%d %B %Y, %H:%M (%Z)")
          echo "time=$now" >> $GITHUB_OUTPUT

      # 4️⃣ Отправляем отчет в Telegram (если работает)
      - name: Notify if container is running
        if: ${{ steps.parse.outputs.status == 'running' }}
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO }}
          token: ${{ secrets.BOT_TOKEN }}
          format: markdown
          message: |
            ✅ *Контейнер работает нормально!*
            🐳 *Имя:* `${{ env.CONTAINER_NAME }}`
            🟢 ${{ steps.parse.outputs.details }}
            🕒 *Время:* ${{ steps.time.outputs.time }}

      # 5️⃣ Отправляем отчет в Telegram (если не работает)
      - name: Notify if container is stopped or not found
        if: ${{ steps.parse.outputs.status != 'running' }}
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO }}
          token: ${{ secrets.BOT_TOKEN }}
          format: markdown
          message: |
            ⚠️ *Контейнер не работает!*
            🐳 *Имя:* `${{ env.CONTAINER_NAME }}`
            🔴 ${{ steps.parse.outputs.details }}
            🕒 *Время:* ${{ steps.time.outputs.time }}
            🔗 [Открыть логи в GitHub Actions](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})