name: Daily Container Check

on:
  schedule:
    - cron: '2 * * * *'
  workflow_dispatch:

env:
  TELEGRAM_TO: -1003170352623
  CONTAINER_NAME: "bot-ebal-nahui"

jobs:
  container-health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check container via SSH
        id: container-check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SSH_PRIVATE_KEY }} 
          script: |
            CONTAINER="${{ env.CONTAINER_NAME }}"
            echo "🔍 Проверяю состояние контейнера: $CONTAINER"

            # Проверяем, запущен ли контейнер
            # Проверяем существование контейнера
            if ! docker inspect "$CONTAINER" >/dev/null 2>&1; then
              echo "❌ Container not found"
              echo "status=not_found" >> $GITHUB_OUTPUT
              echo "details=Container $CONTAINER does not exist" >> $GITHUB_OUTPUT
              exit 0
            fi
            
            # Получаем статус контейнера
            STATUS=$(docker inspect --format='{{.State.Status}}' "$CONTAINER")
            
            if [ "$STATUS" = "running" ]; then
              STARTED_AT=$(docker inspect --format='{{.State.StartedAt}}' "$CONTAINER")
              UPTIME=$(docker inspect --format='{{.State.Status}} since {{.State.StartedAt}}' "$CONTAINER")
              RESTART_COUNT=$(docker inspect --format='{{.RestartCount}}' "$CONTAINER")
              
              echo "✅ Container is running"
              echo "status=running" >> $GITHUB_OUTPUT
              echo "details=🟢 Running | Restarts: $RESTART_COUNT | Started: $STARTED_AT" >> $GITHUB_OUTPUT
            else
              EXIT_CODE=$(docker inspect --format='{{.State.ExitCode}}' "$CONTAINER")
              echo "❌ Container is stopped"
              echo "status=stopped" >> $GITHUB_OUTPUT
              echo "details=🔴 Stopped | Exit Code: $EXIT_CODE" >> $GITHUB_OUTPUT
            fi
            
            # Дополнительная информация
            echo "--- System Info ---"
            docker system df
            echo ""
            echo "--- Running Containers ---"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.RunningFor}}"

      # Шаг 2: Отправка отчета в Telegram
      - name: Send Telegram notification
        if: always()  # Отправлять даже при ошибках
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO }}
          token: ${{ secrets.BOT_TOKEN }}
          message: |
            ${{ 
              steps.container-check.outputs.status == 'running' && '✅ СИСТЕМА В НОРМЕ' :
              steps.container-check.outputs.status == 'stopped' && '❌ ПРОБЛЕМА С КОНТЕЙНЕРОМ' :
              '⚠️ КОНТЕЙНЕР НЕ НАЙДЕН'
            }}
            
            🐳 Контейнер: ${{ env.CONTAINER_NAME }}
            📊 Статус: ${{ steps.container-check.outputs.status }}
            ℹ️ Детали: ${{ steps.container-check.outputs.details }}
            
            🕒 Время проверки: ${{ fromJSON('{"date": "'$(date -Iseconds)'"}').date }}
            🔧 Тип запуска: ${{ github.event_name == 'schedule' && 'Автоматически' : 'Вручную' }}
            
            ${{ steps.container-check.outputs.status != 'running' && '🚨 ТРЕБУЕТСЯ ВМЕШАТЕЛЬСТВО!' : '🎉 Все системы работают нормально!' }}
            
            # Дополнительная информация о системе
            echo "---"
            echo "📊 Общая статистика:"
            docker system df
            echo ""
            echo "🖥️ Использование ресурсов:"
            docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}" | head -5

      - name: Send Telegram report
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO }}
          token: ${{ secrets.BOT_TOKEN }}
          message: |
            📊 ДНЕВНОЙ ОТЧЕТ
            
            Контейнер: ${{ env.CONTAINER_NAME }}
            Статус: ${{ steps.container-check.outputs.status }}
            Детали: ${{ steps.container-check.outputs.details }}
            
            Время проверки: ${{ fromJSON('{"date": "'$(date -Iseconds)'"}').date }}
            Запущено: ${{ github.event_name == 'schedule' && 'по расписанию' : 'вручную' }}