name: Container Manager

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Выберете действие'
        required: true
        default: 'stop'
        type: choice
        options:
          - start
          - stop
          - restart
          - ps
      container_name:
        description: 'Имя контейнера'
        required: true
        default: 'time-bot'
        type: string
      pull_latest:
        description: 'Pull latest image before start'
        required: false
        type: boolean
        default: false

env:
  TELEGRAM_TO: -1003170352623
  REGISTRY: ghcr.io
  IMAGE_NAME: bad0request/time-bot  

jobs:
  manage-container:
    runs-on: ubuntu-latest
    steps:
      # Уведомление о начале действия
      - name: Notify action start
        uses: appleboy/telegram-action@master
        with:  # ← исправлено с witch на with
          to: ${{ env.TELEGRAM_TO }}
          token: ${{ secrets.BOT_TOKEN }}
          message: | 
            ${{ github.event.inputs.action == 'stop' && '🛑 Останавливаю контейнер' || '' }}
            ${{ github.event.inputs.action == 'start' && '🚀 Запускаю контейнер' || '' }}
            ${{ github.event.inputs.action == 'restart' && '🔄 Перезапускаю контейнер' || '' }}
            ${{ github.event.inputs.action == 'ps' && '📊 Проверяю контейнер' || '' }}
            📦 Container: ${{ github.event.inputs.container_name }}
            ${{ github.event.inputs.pull_latest && '📥 Pull latest image: YES' || '' }}
            👤 User: ${{ github.actor }}

      - name: Execute container action via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            CONTAINER="${{ github.event.inputs.container_name }}"
            ACTION="${{ github.event.inputs.action }}"
            PULL_LATEST="${{ github.event.inputs.pull_latest }}"

            echo "🎯 Выполняю: $ACTION для контейнера: $CONTAINER"
            
            case $ACTION in
              "start")
                if [ "$PULL_LATEST" = "true" ]; then
                  echo "📥 Pulling latest image..."
                  docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
                fi
                
                if docker ps -a | grep -q $CONTAINER; then
                  echo "🔁 Контейнер существует, запускаю..."
                  docker start $CONTAINER
                else
                  echo "🚀 Создаю и запускаю новый контейнер..."
                  docker run -d \
                    --name $CONTAINER \
                    ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
                fi
                echo "✅ Контейнер запущен"
                ;;
                
              "stop")
                if docker ps -a | grep -q $CONTAINER; then
                  echo "🛑 Останавливаю контейнер..."
                  docker stop $CONTAINER
                  echo "✅ Контейнер остановлен"
                else
                  echo "⚠️ Контейнер не найден"
                fi
                ;;
                
              "restart")
                if docker ps -a | grep -q $CONTAINER; then
                  echo "🔄 Перезапускаю контейнер..."
                  docker restart $CONTAINER
                  echo "✅ Контейнер перезапущен"
                else
                  echo "⚠️ Контейнер не найден"
                fi
                ;;
                
              "ps")
                echo "📊 Статус контейнера:"
                docker ps -a | grep $CONTAINER || echo "❌ Контейнер не найден"
                ;;
            esac
            
            # Всегда показываем статус после действия
            echo "📋 Текущие контейнеры:"
            docker ps -a

      # Уведомление об успехе
      - name: Notify action success
        if: success()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO }}
          token: ${{ secrets.BOT_TOKEN }}
          message: |
            ✅ Действие выполнено!
            🎯 Action: ${{ github.event.inputs.action }}
            📦 Container: ${{ github.event.inputs.container_name }}
            ${{ github.event.inputs.pull_latest && '📥 Latest image pulled' || '' }}
            👤 User: ${{ github.actor }}

      # Уведомление об ошибке
      - name: Notify action failure
        if: failure()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO }}
          token: ${{ secrets.BOT_TOKEN }}
          message: |
            ❌ Ошибка при выполнении действия
            🎯 Action: ${{ github.event.inputs.action }}
            📦 Container: ${{ github.event.inputs.container_name }}
            ⚠️ Check: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}